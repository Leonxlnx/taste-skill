import "./style.css";

export const metadata = { title: "Common Field — Furniture", description: "Independent furniture studio portfolio." };

export default function Layout({ children }) {
  return <html lang="en"><body>{children}</body></html>;
}
