import './style.css'
export const metadata={title:'Common Table Cooperative',description:'A neighborhood-owned food cooperative.'}
export default function Layout({children}){return <html lang="en"><body>{children}</body></html>}
