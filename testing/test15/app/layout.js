import './style.css'
export const metadata={title:'Low Room Records — Independent label archive',description:'A fictional editorial archive demonstrating an independent record label interface.'}
export default function Layout({children}){return <html lang="en"><body>{children}</body></html>}
