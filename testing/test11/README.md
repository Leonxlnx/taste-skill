# Vector Field — Taste Skill V2 regression test 11

A React + Next.js cybersecurity research lab concept site.

```bash
npm install
npm run dev
npm run build
```

The site intentionally avoids fabricated proof and decorative heading prelabels. The contact address uses the reserved `.example` domain and is not operational. See `PROVENANCE.md` for the generated raster and verified Taste Blocks component.
