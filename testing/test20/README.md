# NOMA H1 launch concept

```bash
npm install
npm run dev
npm run build
npm start
```

Open `http://localhost:3000`. The page is an independent product-launch design concept; commercial specifications and availability are deliberately not claimed.
